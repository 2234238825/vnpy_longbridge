# 家庭基金管理系统开发需求文档

## 1. 项目背景

希望开发一个家庭基金管理系统，用于统一管理家庭成员共同投资的资金。

系统的核心目标不是做一个简单的股票账户查询工具，而是建立一个类似“小型基金”的家庭资产管理系统：

> **资金来源清晰、资产状态清晰、交易记录清晰、基金净值清晰、成员收益清晰，并且所有资产变化都可以追溯。**

家庭成员可以通过 Web 页面查看基金的：

* 总资产
* 当前净值
* 累计收益
* 收益率
* 每日盈亏
* 净值曲线
* 资产配置
* 股票/ETF/债券等持仓
* 交易记录
* 每个家庭成员的投入和收益

后续接入 Longbridge API，实现股票持仓和行情自动同步。

---

# 2. 核心设计理念

## 2.1 基金份额制度

系统不要简单按照：

```text
爸爸投入 20 万
妈妈投入 10 万
HHH投入 5 万
```

计算每个人收益。

而是采用**基金份额制度**。

基金成立时：

```text
基金资产：100,000
基金份额：100,000
基金初始净值：1.0000
```

爸爸投入：

```text
100,000
```

获得：

```text
100,000 份
```

如果之后基金资产上涨到：

```text
120,000
```

那么：

```text
总份额 = 100,000
基金净值 = 1.2000
```

此时妈妈投入：

```text
60,000
```

按照当前净值 1.2 买入：

```text
60,000 / 1.2 = 50,000 份
```

此时：

```text
爸爸：100,000份
妈妈： 50,000份

总份额：150,000
基金资产：180,000
基金净值：1.2000
```

这样新增资金不会被错误计算为基金收益。

### 必须支持

* 成员追加资金
* 成员赎回资金
* 基金分红
* 基金收益
* 成员份额变化
* 成员独立收益率

---

# 3. 系统总体架构

建议使用：

```text
Frontend
    │
    ▼
Vue / React
    │
    ▼
FastAPI
    │
    ├──────────────┐
    ▼              ▼
PostgreSQL      Longbridge API
    │              │
    │              ├── 股票持仓
    │              ├── 股票价格
    │              ├── 现金
    │              └── 账户信息
    │
    ▼
基金会计系统
    │
    ├── 基金净值
    ├── 成员份额
    ├── 持仓
    ├── 交易
    └── 收益
```

### 技术栈

后端：

* Python
* FastAPI
* SQLAlchemy
* PostgreSQL
* Pydantic

前端：

* Vue 3 或 React
* TypeScript
* ECharts

数据：

* Longbridge API
* 手工录入

---

# 4. 用户角色

第一版设计三个角色。

### ADMIN

管理员。

权限：

* 查看全部资产
* 查看全部成员
* 添加成员
* 修改基金信息
* 录入资金
* 录入交易
* 修改资产
* 查看审计日志
* 管理 Longbridge 同步

### MEMBER

普通家庭成员。

权限：

* 查看基金总资产
* 查看基金净值
* 查看基金收益
* 查看资产配置
* 查看持仓
* 查看交易记录
* 查看自己的资金和份额

默认不能修改核心数据。

### VIEWER

只读用户。

只能：

* 查看基金总览
* 查看净值
* 查看资产配置
* 查看持仓

---

# 5. 数据库设计

## 5.1 fund

基金基本信息。

```sql
fund
----
id
name
initial_nav
created_at
updated_at
status
```

示例：

```text
id: 1
name: HHH家庭基金
initial_nav: 1.0000
status: ACTIVE
```

---

## 5.2 member

家庭成员。

```sql
member
------
id
name
username
password_hash
role
created_at
updated_at
```

例如：

```text
爸爸   ADMIN
妈妈   MEMBER
HHH    ADMIN
```

---

## 5.3 fund_member

记录成员在基金中的份额。

```sql
fund_member
-----------
id
fund_id
member_id
shares
total_deposit
total_withdraw
created_at
updated_at
```

---

## 5.4 asset

资产定义。

```sql
asset
-----
id
symbol
name
asset_type
currency
exchange
created_at
updated_at
```

支持：

```text
STOCK
ETF
BOND
CASH
FUND
OTHER
```

例如：

```text
MSFT
0700.HK
510300
US10Y
CASH
```

---

# 6. 持仓

```sql
position
--------
id
fund_id
asset_id
quantity
avg_price
cost
market_value
updated_at
```

记录当前基金持有的资产。

例如：

```text
Microsoft
数量：10
成本：4000 USD
市值：5000 USD
浮盈：1000 USD
```

---

# 7. 交易记录

所有交易必须记录。

```sql
transaction
-----------
id
fund_id
member_id
asset_id
type
quantity
price
amount
currency
fee
trade_time
created_at
```

交易类型至少支持：

```text
BUY
SELL
DEPOSIT
WITHDRAW
DIVIDEND
INTEREST
FEE
TRANSFER
```

例如：

```text
2026-08-18

HHH
BUY
MSFT
10股
$500
总金额 $5,000
```

---

# 8. 基金净值

每天至少保存一次净值。

```sql
fund_nav
--------
id
fund_id
date
total_asset
total_liability
net_asset
shares
nav
daily_return
created_at
```

例如：

| 日期   |    净资产 |    总份额 |     净值 |
| ---- | -----: | -----: | -----: |
| 8/15 | 300000 | 200000 | 1.5000 |
| 8/16 | 302000 | 200000 | 1.5100 |
| 8/17 | 305000 | 200000 | 1.5250 |
| 8/18 | 303000 | 200000 | 1.5150 |

---

# 9. 基金净值计算

核心公式：

```text
净资产 = 总资产 - 总负债
```

基金净值：

```text
NAV = 净资产 / 总份额
```

成员资产：

```text
成员资产 = 成员份额 × 当前NAV
```

成员收益：

```text
收益 = 当前资产 - 净投入资金
```

但是如果存在赎回、分红等情况，收益计算必须基于基金份额/交易流水，而不能简单使用：

```text
当前资产 - 历史累计充值
```

需要建立完整的基金会计逻辑。

---

# 10. 首页 Dashboard

首页是系统最重要的页面。

设计类似：

```text
┌─────────────────────────────────────┐
│          HHH家庭基金                │
│                                     │
│   当前净值                          │
│   1.5832                            │
│                                     │
│   总资产        ¥386,521            │
│   累计收益      +¥86,521            │
│   收益率        +28.84%             │
│                                     │
│   今日盈亏      +¥1,284             │
│   今日收益率      +0.33%            │
└─────────────────────────────────────┘
```

---

# 11. 净值曲线

使用 ECharts。

显示：

```text
基金净值
基金累计收益
每日收益
```

默认显示：

```text
1个月
3个月
6个月
1年
全部
```

---

# 12. 资产配置

显示：

```text
股票       62%
债券       25%
现金        8%
其他        5%
```

使用饼图/环形图。

点击某个资产类别后可以查看详细持仓。

---

# 13. 持仓页面

显示：

| 资产   |  数量 |   成本 | 当前价格 |    市值 |     盈亏 |  盈亏率 |
| ---- | --: | ---: | ---: | ----: | -----: | ---: |
| MSFT |  10 | $400 | $500 | $5000 | +$1000 | +25% |
| XXX  | 100 |  ¥20 |  ¥22 | ¥2200 |  +¥200 | +10% |

支持：

* 股票
* ETF
* 债券
* 现金
* 其他资产

---

# 14. 家庭成员页面

显示每个人的基金份额。

例如：

| 成员  |     累计投入 |     当前资产 |      收益 |    收益率 |     份额 |
| --- | -------: | -------: | ------: | -----: | -----: |
| 爸爸  | ¥100,000 | ¥128,300 | ¥28,300 | 28.30% | 100000 |
| 妈妈  |  ¥60,000 |  ¥76,980 | ¥16,980 | 28.30% |  50000 |
| HHH |  ¥80,000 | ¥102,664 | ¥22,664 | 28.33% |  30000 |

需要注意：

**成员收益率必须基于基金份额计算，而不是简单用当前资产/累计投入。**

---

# 15. 交易页面

提供完整交易流水：

```text
时间
成员
类型
资产
数量
价格
金额
手续费
备注
```

支持筛选：

```text
成员
资产
交易类型
时间范围
```

---

# 16. Longbridge 自动同步

后续接入 Longbridge API。

系统定时同步：

```text
Longbridge
    │
    ├── 账户现金
    ├── 股票持仓
    ├── 股票价格
    ├── 市值
    └── 盈亏
          │
          ▼
     同步服务
          │
          ▼
      PostgreSQL
          │
          ▼
       基金净值
```

建议设计独立的：

```text
LongbridgeService
```

不要把 Longbridge API 调用直接写到 FastAPI Controller 中。

---

# 17. 数据同步原则

Longbridge 是**外部数据源**。

数据库是基金系统自己的**账本**。

不能每次打开网页都直接拿 Longbridge 数据重新计算所有历史数据。

应该：

```text
Longbridge
    ↓
同步
    ↓
数据库保存快照
    ↓
基金系统计算
    ↓
生成净值
```

这样即使未来：

* Longbridge API 改变
* 更换券商
* API 暂时不可用

历史基金数据仍然完整。

---

# 18. 审计日志

家庭资金管理中，这个功能非常重要。

建立：

```sql
audit_log
---------
id
user_id
action
object_type
object_id
old_value
new_value
created_at
```

例如：

```text
2026-08-18 18:32

用户：HHH

操作：修改资产

现金
100000 → 95000

备注：
买入 MSFT 10股
```

原则：

> **所有影响基金资产、份额、交易和净值的数据修改都必须可追溯。**

---

# 19. 权限和安全

必须实现：

* JWT 登录
* 密码哈希
* RBAC 权限
* API 权限检查
* 管理员接口保护
* 审计日志
* 敏感配置使用环境变量

例如：

```env
DATABASE_URL=
LONGBRIDGE_APP_KEY=
LONGBRIDGE_APP_SECRET=
JWT_SECRET=
```

**禁止把密钥直接写进 Git。**

---

# 20. 第一阶段 MVP

第一阶段不要接 Longbridge。

先实现：

```text
登录
 ↓
基金
 ↓
家庭成员
 ↓
资金投入
 ↓
基金份额
 ↓
资产
 ↓
交易
 ↓
持仓
 ↓
净值
 ↓
Dashboard
```

所有数据允许手工录入。

目标是先验证：

> **基金份额、资产、交易、净值之间的账务逻辑正确。**

---

# 21. 第二阶段

接入 Longbridge：

```text
自动同步：

股票持仓
股票价格
现金
账户资产
```

然后自动计算：

```text
基金总资产
基金净值
每日收益
成员收益
```

---

# 22. 第三阶段

增加量化分析：

```text
累计收益率
年化收益率
最大回撤
Sharpe Ratio
Alpha
Beta
波动率
胜率
```

同时增加：

```text
资产配置分析
年度收益
月度收益
成员收益
```

---

# 23. 第四阶段

增加通知：

```text
每日基金报告

HHH家庭基金

净值：1.5832
今日：+0.33%
本月：+2.18%
今年：+8.32%

总资产：¥386,521

股票：62%
债券：25%
现金：8%
其他：5%
```

可以后续接：

* Telegram
* 企业微信
* 邮件
* 微信机器人等

---

# 24. 项目目录建议

```text
family-fund/
│
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   │
│   │   ├── api/
│   │   │   ├── auth.py
│   │   │   ├── fund.py
│   │   │   ├── member.py
│   │   │   ├── asset.py
│   │   │   ├── position.py
│   │   │   ├── transaction.py
│   │   │   ├── nav.py
│   │   │   └── dashboard.py
│   │   │
│   │   ├── models/
│   │   ├── schemas/
│   │   ├── services/
│   │   │   ├── fund_service.py
│   │   │   ├── nav_service.py
│   │   │   ├── position_service.py
│   │   │   ├── transaction_service.py
│   │   │   └── longbridge_service.py
│   │   │
│   │   ├── repositories/
│   │   ├── core/
│   │   └── utils/
│   │
│   ├── migrations/
│   └── tests/
│
├── frontend/
│   ├── src/
│   │   ├── views/
│   │   ├── components/
│   │   ├── api/
│   │   └── router/
│   └── package.json
│
├── docker-compose.yml
├── README.md
└── .env.example
```

---

# 25. 开发原则

Claude Code 开发时需要遵守以下原则：

### 原则一：先账务，后 UI

优先保证：

```text
资金
↓
份额
↓
交易
↓
持仓
↓
净值
↓
收益
```

逻辑正确。

---

### 原则二：所有金额使用 Decimal

不要使用 Python `float` 处理资金。

例如：

```python
from decimal import Decimal
```

数据库金额字段使用：

```text
NUMERIC / DECIMAL
```

避免：

```text
0.1 + 0.2 != 0.3
```

这种浮点数问题污染基金账务。

---

### 原则三：交易不可随意删除

原则上交易记录只能：

```text
新增
冲正
```

不要直接：

```text
DELETE
```

这样才能保证审计完整。

---

### 原则四：基金净值不能直接修改

净值应该由：

```text
资产
+
负债
+
份额
```

计算得到。

而不是让管理员手工填写：

```text
今天净值 = 1.58
```

---

### 原则五：Longbridge 与基金系统解耦

Longbridge 是行情/账户数据来源。

不要让核心基金会计逻辑依赖 Longbridge。

未来应该可以替换成：

```text
Longbridge
        │
Interactive Brokers
        │
其他券商
        │
手工录入
        ↓
统一 Portfolio Adapter
        ↓
基金系统
```

---

# 26. 最终目标

最终系统应该让家庭成员打开网页后，一眼看到：

```text
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
        HHH 家庭基金
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

基金净值

       1.5832

总资产

      ¥386,521

累计收益

      +¥86,521
      +28.84%

今日收益

      +¥1,284
      +0.33%

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

        📈 基金净值曲线

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

资产配置

股票 ████████████████ 62%
债券 ███████          25%
现金 ███               8%
其他 ██                5%

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

家庭成员

爸爸    ¥128,300   +28.30%
妈妈     ¥76,980   +28.30%
HHH     ¥102,664   +28.33%

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
```

最终形成一个：

> **家庭版基金管理 + 家庭资产透明化 + 投资组合分析系统**

而不是单纯的股票行情网页。

---

## 给 Claude Code 的执行要求

你可以把下面这段直接放在这份文档最后：

> **请根据以上需求开发项目。**
>
> 开发时不要一次性生成所有代码。请采用迭代方式：
>
> 1. 首先设计数据库 Schema 和核心领域模型。
> 2. 实现基金份额、资金申购/赎回、交易、持仓、净值计算核心逻辑。
> 3. 编写单元测试，重点测试基金份额和净值计算。
> 4. 再实现 FastAPI API。
> 5. 再实现前端 Dashboard。
> 6. 最后接入 Longbridge。
>
> 每完成一个阶段先运行测试并检查已有功能，不要为了实现新功能破坏已有功能。
>
> 所有金融金额使用 Decimal。
>
> 核心账务逻辑必须有完整的单元测试。
>
> 不要把 Longbridge API 调用直接耦合到基金核心业务逻辑。
>
> 所有交易和资产变化必须可追溯。
>
> 如果需求存在歧义，优先采用能够保证**账务准确、数据可追溯、未来可扩展**的设计，并在实现前说明设计决策。

---

# 27. 设计决策（启动前补充）

以下为结合**家庭系统规模**与现有 **vnpy_longbridge 项目**后确定的技术选型调整。
核心账务设计（基金份额、Decimal、净值计算、可追溯）**全部保留**，仅调整工程实现层面。

## 27.1 数据库：MVP 用 SQLite，PostgreSQL 后置

| 项 | 决策 |
|----|------|
| MVP | **SQLite**（零运维，家庭系统数据量小，完全够用） |
| 迁移 | 保留 SQLAlchemy ORM，日后切 PostgreSQL 只改 `DATABASE_URL`，无需改业务代码 |

## 27.2 前端：MVP 轻量，Vue 后置

| 项 | 决策 |
|----|------|
| MVP | **FastAPI 静态页 + ECharts**（参考 vnpy_longbridge 的 web 页面做法，单文件/少量 JS） |
| 后置 | 页面复杂后（多页面、复杂交互）再引入 Vue 3 + TypeScript 独立工程 |
| 前提 | 若开发者熟悉 Vue，可跳过轻量阶段直接上 Vue |

## 27.3 目录：去掉 repositories 层

原文档 24 节含 `repositories`，对家庭系统属过度分层。简化为：

```text
family-fund/
├── backend/
│   ├── app/
│   │   ├── main.py
│   │   ├── core/          # 配置、安全（JWT）
│   │   ├── models/        # SQLAlchemy 模型
│   │   ├── schemas/       # Pydantic 校验
│   │   ├── services/      # 业务逻辑（基金/净值/交易/份额）
│   │   │   └── adapters/  # PortfolioAdapter（手工录入 / 长桥）
│   │   └── api/           # 路由（auth/fund/member/asset/position/transaction/nav/dashboard）
│   ├── migrations/
│   └── tests/
├── frontend/              # MVP 可先并入 backend 静态页
├── docker-compose.yml
├── README.md
└── .env.example
```

## 27.4 与 vnpy_longbridge 的关系

- **family-fund 是独立项目**（独立目录/仓库），与 vnpy_longbridge 生命周期不同，不互相依赖代码。
- **复用长桥 OAuth token**（已持久化在 `~/.longbridge/`），第二阶段同步**直接调长桥 SDK**，不引入整个 vnpy 框架。
- 可选：vnpy_longbridge 的 `data_service` 下载的历史价格可为净值计算提供标的价格（替代实时长桥）。

## 27.5 PortfolioAdapter：MVP 就定义接口

呼应文档"原则五：Longbridge 与基金系统解耦"。MVP 阶段即定义统一接口，两个实现：

```text
PortfolioAdapter
    ├── ManualAdapter      # 手工录入（MVP 用）
    └── LongbridgeAdapter  # 长桥自动同步（第二阶段）
```

核心基金账务逻辑只依赖 `PortfolioAdapter` 接口，不感知具体数据来源，未来可扩展其它券商。

## 27.6 保留不变的核心设计

1. 基金份额制度（按净值买入份额，新增资金不污染已有成员收益）
2. 所有金额用 `Decimal`，数据库字段用 `NUMERIC/DECIMAL`
3. 净值由资产/负债/份额**计算**，不手工填写
4. 交易只能**新增 / 冲正**，不直接 DELETE
5. 审计日志记录所有资产/份额/交易/净值变更
6. JWT 登录 + RBAC 三角色 + 敏感配置走环境变量
7. 迭代开发：Schema → 核心账务 + 单测 → API → 前端 → Longbridge



 